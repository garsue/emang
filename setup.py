#!/usr/bin/env python
#vim: fileencoding=utf-8

from setuptools import setup, find_packages
setup(
        name = "garsue_style",
        version = "1.0",
        packages = find_packages(),
        )
