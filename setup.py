#!/usr/bin/env python
#vim: fileencoding=utf-8

from setuptools import setup, find_packages
setup(
        name = "GarSue-Style",
        version = "1.2",
        packages = find_packages(),
        scripts = ["gstyle"]
        )
